import Component from "$store/components/product/PixPrice.tsx";
import type { Props } from "$store/components/product/PixPrice.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
