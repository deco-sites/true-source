import Component from "$store/components/product/ProductStars.tsx";
import type { Props } from "$store/components/product/ProductStars.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
