import Component from "deco-sites/true-source/components/product/OutOfStock.tsx";
import type { Props } from "deco-sites/true-source/components/product/OutOfStock.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
