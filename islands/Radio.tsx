import Component from "$store/components/ui/Radio.tsx";
import type { Props } from "$store/components/ui/Radio.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
