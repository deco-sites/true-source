import Component from "deco-sites/true-source/components/ui/Radio.tsx";
import type { Props } from "deco-sites/true-source/components/ui/Radio.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
